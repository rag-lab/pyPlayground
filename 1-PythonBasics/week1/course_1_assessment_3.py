#What are correct ways to tell a turtle named Tex 
#to move forward 20 pixels? Select as many as apply.
Tex.forward(20)
Tex.forward(10 + 10)

#Which is the correct way to make a new instance of the Turtle class?
turtle.Turtle()

#What does each instance of the Turtle class represent?
A unique 'turtle' that you can use to draw.

#True or False, attributes/instance variables are just like other variables in Python. 
True

#Select all of the following things that methods can do:
Change the value of an attribute.
Return values.
Create new attributes of an instance and set their values.

#For an instance of a class that is assigned to the variable student, 
#what is the proper way to refer to the title attribute/instance variable?
student.title

#What is the name of jane’s attribute (not method) that is referred to 
#in the following code?
import turtle

jane = turtle.Turtle()
jane.forward(20)
print(jane.x)

The attribute is X

#What are the names of the instances in the following code? 
# Please put one instance per blank space and enter them 
# in the order that the computer would read them.
import turtle
wn = turtle.Screen()

jazz = turtle.Turtle()
jazz.forward(50)
jazz.right(90)
pop = turtle.Turtle()
pop.left(180)
pop.forward(76)

wn / jazz / pop

